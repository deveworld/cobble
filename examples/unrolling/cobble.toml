[project]
name = "cobble_unrolling"
description = "Cobble compile-time unrolling fixture"
namespace = "cobble_unrolling"
version = "1.0.0"
pack_format = "101.1"

[build]
source = "src"
output = "output"
entry_points = ["main.cbl"]
